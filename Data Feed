import requests

def fetch_live_data(ticker):
    api_key = 'YOUR_API_KEY'
    url = f'https://api.example.com/v1/quote/{ticker}'
    response = requests.get(url, headers={'Authorization': f'Bearer {api_key}'})
    return response.json()
